---
title: "useControllableState"
description: "A hook for a value that can be either controlled or uncontrolled, with a single consistent API either way."
---

import { useControllableStateApi } from "@/lib/api-data/use-controllable-state"

Powers every component in this registry that supports both a `value` / `onValueChange` (controlled) and a `defaultValue` (uncontrolled) mode — including [Price Input](/docs/components/price-input), [Elastic Slider](/docs/components/elastic-slider), and [Elastic Range Slider](/docs/components/elastic-range-slider).

<LastEdited path="apps/web/content/docs/utilities/use-controllable-state.mdx" />

## Overview

The first counter manages its own state internally (uncontrolled). The second is driven entirely by the parent (controlled) — the same `Counter` implementation handles both.

<ComponentPreview name="use-controllable-state-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/use-controllable-state" sourceName="use-controllable-state" sourceKind="hook" sourceTitle="hooks/use-controllable-state.ts" sourceStep="Copy the source into your project" />

## Usage

```tsx
import { useControllableState } from "@/hooks/use-controllable-state"

function Counter({ value, defaultValue = 0, onValueChange }) {
  const [count, setCount] = useControllableState({
    prop: value,
    defaultProp: defaultValue,
    onChange: onValueChange,
    caller: "Counter",
  })

  return <button onClick={() => setCount((c) => c + 1)}>{count}</button>
}
```

## API Reference

<ApiReference title="useControllableState(options)" rows={useControllableStateApi} />
