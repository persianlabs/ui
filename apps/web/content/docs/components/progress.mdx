---
title: "Progress"
description: "Displays an indicator showing the completion progress of a task, built on Base UI."
---

import { progressRootApi } from "@/lib/api-data/progress"

Displays an indicator showing the completion progress of a task. Built on Base UI's progress primitive.

<LastEdited path="apps/web/content/docs/components/progress.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed={false}
        />

## Overview

<ComponentPreview name="progress-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/progress" packages="@base-ui/react" sourceName="progress" sourceTitle="components/ui/progress.tsx" />

## Usage

```tsx
import { Progress } from "@/components/ui/progress"

export function Example() {
  return <Progress value={66} />
}
```

## Examples

### Animated

Update `value` over time to animate the indicator.

<ComponentPreview name="progress-animated" />

### RTL

<ComponentPreview name="progress-rtl" direction="rtl" />

## API Reference

<ApiReference title="Progress" rows={progressRootApi} />
