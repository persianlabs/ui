---
title: "Resizable"
description: "Accessible resizable panel groups and layouts, built on react-resizable-panels."
---

import { resizableHandleApi, resizablePanelApi, resizablePanelGroupApi } from "@/lib/api-data/resizable"

Accessible resizable panel groups and layouts, built on `react-resizable-panels`.

<LastEdited path="apps/web/content/docs/components/resizable.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed={false}
        />

## Overview

<ComponentPreview name="resizable-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/resizable" packages="react-resizable-panels" sourceName="resizable" sourceTitle="components/ui/resizable.tsx" />

## Usage

```tsx
import {

  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@/components/ui/resizable"

export function Example() {
  return (
    <ResizablePanelGroup direction="horizontal" className="h-40">
      <ResizablePanel defaultSize={50}>Sidebar</ResizablePanel>
      <ResizableHandle withHandle />
      <ResizablePanel defaultSize={50}>Content</ResizablePanel>
    </ResizablePanelGroup>
  )
}
```

## Examples

### Vertical

<ComponentPreview name="resizable-vertical" />

### RTL

The drag handle's centering is direction-agnostic, and the panel order visually flips along with the surrounding layout under RTL.

<ComponentPreview name="resizable-rtl" direction="rtl" />

## API Reference

<ApiReference title="ResizablePanelGroup" rows={resizablePanelGroupApi} />

<ApiReference title="ResizablePanel" rows={resizablePanelApi} />

<ApiReference title="ResizableHandle" rows={resizableHandleApi} />
