---
title: "Button"
description: "A button built on Base UI with variant and size support."
---

import { buttonRootApi } from "@/lib/api-data/button"

Displays a button, or a component that looks like a button. Built on Base UI's button primitive.

<LastEdited path="apps/web/content/docs/components/button.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={["Added the `loading` prop", "Added the `blue` variant"]}
        />

## Overview

<ComponentPreview name="button-default" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/button" packages="@base-ui/react class-variance-authority" sourceName="button" sourceTitle="components/ui/button.tsx" />

## Usage

```tsx
import { Button } from "@/components/ui/button"

export function Example() {
  return <Button variant="outline">Button</Button>
}
```

## Examples

### Sizes

Pass `size` to change the size of the button.

<ComponentPreview name="button-size" />

### Outline

<ComponentPreview name="button-outline" />

### Secondary

<ComponentPreview name="button-secondary" />

### Ghost

<ComponentPreview name="button-ghost" />

### Destructive

<ComponentPreview name="button-destructive" />

### Link

<ComponentPreview name="button-link" />

### Blue

<ComponentPreview name="button-blue" />

### Icon

<ComponentPreview name="button-icon" />

### With icon

Add `data-icon="inline-start"` or `data-icon="inline-end"` to the icon for the correct spacing.

<ComponentPreview name="button-with-icon" />

### Rounded

Use the `rounded-full` class to make the button rounded.

<ComponentPreview name="button-rounded" />

### Loading

Pass `loading` to show a spinner alongside the button's content and disable interaction. The spinner scales with the button's `size`.

<ComponentPreview name="button-loading" />

### As link

Use the `buttonVariants` helper to make a link look like a button. Base UI's `Button` always applies `role="button"`, which overrides the semantic link role on `<a>` elements — use a plain `<a>` with `buttonVariants` instead.

<ComponentPreview name="button-render" />

### Button Group

To create a button group, use the [ButtonGroup](/docs/components/button-group) component.

<ComponentPreview name="button-group-demo" />

### RTL

<ComponentPreview name="button-rtl" direction="rtl" />

## API Reference

<ApiReference title="Button" rows={buttonRootApi} />
