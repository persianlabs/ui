---
title: "Textarea"
description: "Displays a form textarea or a component that looks like a textarea."
---

import { textareaRootApi } from "@/lib/api-data/textarea"

Displays a form textarea, or a component that looks like a textarea.

<LastEdited path="apps/web/content/docs/components/textarea.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed={false}
        />

## Overview

<ComponentPreview name="textarea-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/textarea" sourceName="textarea" sourceTitle="components/ui/textarea.tsx" />

## Usage

```tsx
import { Textarea } from "@/components/ui/textarea"

export function Example() {
  return <Textarea placeholder="Type your message here." />
}
```

## Examples

### Field

Use `Field`, `FieldLabel`, and `FieldDescription` to create a textarea with a label and description.

<ComponentPreview name="textarea-field" />

### Disabled

Use the `disabled` prop to disable the textarea. Add `data-disabled` to `Field` to style the disabled label too.

<ComponentPreview name="textarea-disabled" />

### Invalid

Use `aria-invalid` to mark the textarea as invalid, and `data-invalid` on `Field` to style the invalid label.

<ComponentPreview name="textarea-invalid" />

### Button

Pair with `Button` to create a textarea with a submit button.

<ComponentPreview name="textarea-button" />

### RTL

<ComponentPreview name="textarea-rtl" direction="rtl" />

## API Reference

<ApiReference title="Textarea" rows={textareaRootApi} />
