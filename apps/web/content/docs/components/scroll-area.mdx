---
title: "Scroll Area"
description: "Cross-browser custom scrollbars, built on Base UI."
---

import { scrollAreaApi } from "@/lib/api-data/scroll-area"

Augments native scroll functionality for custom, cross-browser styling. Built on Base UI's scroll area primitive.

<LastEdited path="apps/web/content/docs/components/scroll-area.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed={false}
        />

## Overview

<ComponentPreview name="scroll-area-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/scroll-area" packages="@base-ui/react shadcn" sourceName="scroll-area" sourceTitle="components/ui/scroll-area.tsx" />

## Usage

```tsx
import { ScrollArea } from "@/components/ui/scroll-area"

export function Example() {
  return (
    <ScrollArea className="h-56 w-48 rounded-lg border">
      {/* long content */}
    </ScrollArea>
  )
}
```

## Examples

### Horizontal

Set `scrollbarOrientation="horizontal"` for a horizontally scrolling row.

<ComponentPreview name="scroll-area-horizontal" />

### RTL

The vertical scrollbar uses a logical `border-s` rather than a physical side, so it correctly sits on the visual left when the viewport is RTL.

<ComponentPreview name="scroll-area-rtl" direction="rtl" />

## API Reference

<ApiReference title="ScrollArea" rows={scrollAreaApi} />
