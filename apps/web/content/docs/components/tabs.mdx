---
title: "Tabs"
description: "An accessible, animated tabs component with a spring-driven sliding indicator, built on Base UI and Motion."
---

import { tabsContentApi, tabsListApi, tabsRootApi, tabsTriggerApi } from "@/lib/api-data/tabs"

A set of layered panels, one visible at a time, with an active-tab indicator that slides to match the current selection using a spring animation. Built on Base UI's tabs primitive, so focus management, keyboard navigation, and ARIA wiring come for free.

<LastEdited path="apps/web/content/docs/components/tabs.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={[
            "Added enter/exit animations powered by Motion (motion.dev)",
          ]}
        />

## Overview

<ComponentPreview name="tabs-default" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/tabs" packages="@base-ui/react motion" sourceName="tabs" sourceTitle="components/ui/tabs.tsx" />

## Usage

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"

export function Example() {
  return (
    <Tabs defaultValue="overview">
      <TabsList>
        <TabsTrigger value="overview">Overview</TabsTrigger>
        <TabsTrigger value="activity">Activity</TabsTrigger>
      </TabsList>
      <TabsContent value="overview">Overview content</TabsContent>
      <TabsContent value="activity">Activity content</TabsContent>
    </Tabs>
  )
}
```

## Examples

### Variants

Pass `variant` to `Tabs` to switch the tab list's visual style.

<ComponentPreview name="tabs-variants" />

### With icons

<ComponentPreview name="tabs-icons" />

### Disabled tab

<ComponentPreview name="tabs-disabled" />

### Controlled

<ComponentPreview name="tabs-controlled" />

### Vertical

<ComponentPreview name="tabs-vertical" />

### Scrollable

When the tab list overflows, edges fade to hint at more content and sharpen once you've scrolled all the way — direction aware, so it mirrors correctly in RTL.

<ComponentPreview name="tabs-scrollable" />

## API Reference

<ApiReference title="Tabs" rows={tabsRootApi} />

<ApiReference title="TabsList" rows={tabsListApi} />

<ApiReference title="TabsTrigger" rows={tabsTriggerApi} />

<ApiReference title="TabsContent" rows={tabsContentApi} />
