---
title: "Responsive Dialog"
description: "Renders a Dialog on desktop and a bottom Drawer on mobile from one shared set of components."
---

import { responsiveDialogRootApi } from "@/lib/api-data/responsive-dialog"

A modal that adapts to the viewport — a centered [Dialog](/docs/components/dialog) on desktop, a bottom [Drawer](/docs/components/drawer) on mobile, from one shared set of components so you don't maintain two versions of the same form.

<LastEdited path="apps/web/content/docs/components/responsive-dialog.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={[
            "Not a shadcn/ui component — this composition (switch between Dialog and Drawer by viewport width, behind one shared API) is original to this registry, built on top of this registry's own Dialog and Drawer",
            "ResponsiveDialogDesktopOnly hides Cancel/انصراف-style actions on mobile, where the drawer is already dismissed by swiping down, so a redundant Cancel button never has to be built twice",
          ]}
        />

## Overview

Resize the browser window to see the preview switch between a Dialog and a Drawer.

<ComponentPreview name="responsive-dialog-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/responsive-dialog" packages="@base-ui/react" sourceName="responsive-dialog" sourceTitle="components/ui/responsive-dialog.tsx" sourceStep="Copy the Dialog, Drawer, and use-media-query hook first" />

## Usage

```tsx
import {

  ResponsiveDialog,
  ResponsiveDialogClose,
  ResponsiveDialogDescription,
  ResponsiveDialogDesktopOnly,
  ResponsiveDialogFooter,
  ResponsiveDialogHeader,
  ResponsiveDialogPanel,
  ResponsiveDialogPopup,
  ResponsiveDialogTitle,
  ResponsiveDialogTrigger,
} from "@/components/ui/responsive-dialog"

export function Example() {
  return (
    <ResponsiveDialog>
      <ResponsiveDialogTrigger render={<Button variant="outline">Open</Button>} />
      <ResponsiveDialogPopup>
        <ResponsiveDialogHeader>
          <ResponsiveDialogTitle>Edit profile</ResponsiveDialogTitle>
          <ResponsiveDialogDescription>
            A Dialog on desktop, a Drawer on mobile.
          </ResponsiveDialogDescription>
        </ResponsiveDialogHeader>
        <ResponsiveDialogPanel>{/* form fields */}</ResponsiveDialogPanel>
        <ResponsiveDialogFooter>
          <Button type="submit">Save changes</Button>
          {/* Skipped on mobile — the drawer is dismissed by swiping. */}
          <ResponsiveDialogDesktopOnly>
            <ResponsiveDialogClose render={<Button variant="outline">Cancel</Button>} />
          </ResponsiveDialogDesktopOnly>
        </ResponsiveDialogFooter>
      </ResponsiveDialogPopup>
    </ResponsiveDialog>
  )
}
```

## Examples

### Desktop-only actions

Wrap a secondary cancel action with `ResponsiveDialogDesktopOnly`. The primary action remains available everywhere, while mobile users dismiss the drawer by swiping down.

<ComponentPreview name="responsive-dialog-desktop-only" />

### No footer

`ResponsiveDialogFooter` is optional here too — for a purely informational modal, skip it on both the Dialog and Drawer side.

<ComponentPreview name="responsive-dialog-no-footer" />

### RTL

<ComponentPreview name="responsive-dialog-rtl" direction="rtl" />

## API Reference

<ApiReference title="ResponsiveDialog" rows={responsiveDialogRootApi} />

<BaseUiReference
          href="https://base-ui.com/react/components/dialog"
          label="Dialog API"
        />
<BaseUiReference
          href="https://base-ui.com/react/components/drawer"
          label="Drawer API"
        />
