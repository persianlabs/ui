---
title: "Native Select"
description: "A styled native <select> element for choosing a single value."
---

import { nativeSelectRootApi } from "@/lib/api-data/native-select"

A styled native `<select>` element for choosing a single value — useful when you want native mobile picker behavior instead of [Select](/docs/components/select) 's custom popup.

<LastEdited path="apps/web/content/docs/components/native-select.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={[
            "The dropdown chevron and padding use logical start/end properties, so it sits on the correct side in RTL without an override",
          ]}
        />

## Overview

<ComponentPreview name="native-select-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/native-select" sourceName="native-select" sourceTitle="components/ui/native-select.tsx" />

## Usage

```tsx
import {

  NativeSelect,
  NativeSelectOption,
} from "@/components/ui/native-select"

export function Example() {
  return (
    <NativeSelect defaultValue="next">
      <NativeSelectOption value="next">Next.js</NativeSelectOption>
      <NativeSelectOption value="svelte">SvelteKit</NativeSelectOption>
    </NativeSelect>
  )
}
```

## Examples

### Size

<ComponentPreview name="native-select-size" />

### RTL

<ComponentPreview name="native-select-rtl" direction="rtl" />

## API Reference

<ApiReference title="NativeSelect" rows={nativeSelectRootApi} />
