---
title: "Accordion"
description: "A vertically stacked set of interactive headings that each reveal a section of content, built on Base UI."
---

import { accordionItemApi, accordionRootApi } from "@/lib/api-data/accordion"

A vertically stacked set of interactive headings that each reveal a section of content. Built on Base UI's accordion primitive.

<LastEdited path="apps/web/content/docs/components/accordion.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={[
            "Trigger uses text-start and a single chevron that rotates 180° via aria-expanded, instead of two swapped icons — reads correctly in both LTR and RTL without an icon-mirroring rule",
          ]}
        />

## Overview

<ComponentPreview name="accordion-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/accordion" packages="@base-ui/react" sourceName="accordion" sourceTitle="components/ui/accordion.tsx" />

## Usage

```tsx
import {

  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion"

export function Example() {
  return (
    <Accordion defaultValue={["item-1"]}>
      <AccordionItem value="item-1">
        <AccordionTrigger>Is it accessible?</AccordionTrigger>
        <AccordionContent>
          Yes. It adheres to the WAI-ARIA design pattern.
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  )
}
```

## Examples

### RTL

<ComponentPreview name="accordion-rtl" direction="rtl" />

## API Reference

<ApiReference title="Accordion" rows={accordionRootApi} />

<ApiReference title="AccordionItem" rows={accordionItemApi} />
