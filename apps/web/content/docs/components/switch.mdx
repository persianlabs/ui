---
title: "Switch"
description: "A control that toggles between checked and unchecked, built on Base UI."
---

import { switchRootApi } from "@/lib/api-data/switch"

A control that toggles between checked and unchecked. Built on Base UI's switch primitive.

<LastEdited path="apps/web/content/docs/components/switch.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={[
            "The thumb slides toward the reading-end (mirrored in RTL) instead of always sliding right",
          ]}
        />

## Overview

<ComponentPreview name="switch-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/switch" packages="@base-ui/react" sourceName="switch" sourceTitle="components/ui/switch.tsx" />

## Usage

```tsx
import { Switch } from "@/components/ui/switch"

export function Example() {
  return <Switch />
}
```

## Examples

### Size

Use `size="sm"` for a smaller switch.

<ComponentPreview name="switch-size" />

### RTL

<ComponentPreview name="switch-rtl" direction="rtl" />

## API Reference

<ApiReference title="Switch" rows={switchRootApi} />
