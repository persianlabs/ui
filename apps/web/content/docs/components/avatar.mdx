---
title: "Avatar"
description: "An image element with a fallback, for representing a user or entity, built on Base UI."
---

import { avatarFallbackApi, avatarImageApi, avatarRootApi } from "@/lib/api-data/avatar"

An image element with a fallback, for representing a user or entity. Built on Base UI's avatar primitive.

<LastEdited path="apps/web/content/docs/components/avatar.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed={true}
          changes={[
            "Replaced physical right-0 with logical end-0 on AvatarBadge for RTL",
            "Added rtl:space-x-reverse to AvatarGroup so overlap direction flips correctly under RTL",
          ]}
        />

## Overview

<ComponentPreview name="avatar-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/avatar" packages="@base-ui/react" sourceName="avatar" sourceTitle="components/ui/avatar.tsx" />

## Usage

```tsx
import {

  Avatar,
  AvatarFallback,
  AvatarImage,
} from "@/components/ui/avatar"

export function Example() {
  return (
    <Avatar>
      <AvatarImage src="https://github.com/shadcn.png" alt="@shadcn" />
      <AvatarFallback>CN</AvatarFallback>
    </Avatar>
  )
}
```

## Examples

### Size

<ComponentPreview name="avatar-size" />

### Badge

Use `AvatarBadge` to overlay a status indicator, such as an online dot.

<ComponentPreview name="avatar-badge" />

### Group

Use `AvatarGroup` and `AvatarGroupCount` to stack multiple avatars with an overflow count.

<ComponentPreview name="avatar-group" />

### RTL

The badge position and group overlap direction both flip correctly under RTL.

<ComponentPreview name="avatar-rtl" direction="rtl" />

## API Reference

<ApiReference title="Avatar" rows={avatarRootApi} />

<ApiReference title="AvatarImage" rows={avatarImageApi} />

<ApiReference title="AvatarFallback" rows={avatarFallbackApi} />
