---
title: "Combobox"
description: "A searchable, accessible combobox built on Base UI."
---

import { comboboxContentApi, comboboxInputApi, comboboxItemApi, comboboxRootApi } from "@/lib/api-data/combobox"

An input that filters a list of options as you type. Built on Base UI's combobox primitive, so filtering, keyboard navigation, and ARIA wiring come for free. Looking for a ready-made province & city picker? See [City Selector](/docs/components/city-selector).

<LastEdited path="apps/web/content/docs/components/combobox.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed
          changes={["Improved RTL support"]}
        />

## Overview

<ComponentPreview name="combobox-basic" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/combobox" packages="@base-ui/react" sourceName="combobox" sourceTitle="components/ui/combobox.tsx" />

## Usage

```tsx
import {

  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxIcon,
  ComboboxInput,
  ComboboxInputGroup,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"

const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function Example() {
  return (
    <Combobox items={frameworks}>
      <ComboboxInputGroup>
        <ComboboxInput placeholder="Select a framework" />
        <ComboboxIcon />
      </ComboboxInputGroup>
      <ComboboxContent>
        <ComboboxEmpty>No frameworks found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => <ComboboxItem key={item} value={item}>{item}</ComboboxItem>}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## Examples

### Multiple selection

<ComponentPreview name="combobox-multiple" />

### Groups

<ComponentPreview name="combobox-groups" />

### Clear button

<ComponentPreview name="combobox-clear" />

### Disabled

<ComponentPreview name="combobox-disabled" />

## API Reference

<ApiReference title="Combobox" rows={comboboxRootApi} />

<ApiReference title="ComboboxInput" rows={comboboxInputApi} />

<ApiReference title="ComboboxItem" rows={comboboxItemApi} />

<ApiReference title="ComboboxContent" rows={comboboxContentApi} />
