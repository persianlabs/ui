---
title: "Empty"
description: "Displays an empty state with a title, description, and optional actions."
---

import { emptyMediaApi } from "@/lib/api-data/empty"

Use the Empty component to display an empty state, with a title, description, and optional actions.

<LastEdited path="apps/web/content/docs/components/empty.mdx" />

<Credits
          sources={[{ label: "shadcn/ui", href: "https://ui.shadcn.com" }]}
          changed={false}
        />

## Overview

<ComponentPreview name="empty-demo" />

## Installation

<InstallTabs command="npx shadcn@latest add @persianlabsui/empty" packages="class-variance-authority" sourceName="empty" sourceTitle="components/ui/empty.tsx" />

## Usage

```tsx
import {

  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty"

export function Example() {
  return (
    <Empty>
      <EmptyHeader>
        <EmptyMedia variant="icon">
          <Icon />
        </EmptyMedia>
        <EmptyTitle>No data</EmptyTitle>
        <EmptyDescription>No data found</EmptyDescription>
      </EmptyHeader>
      <EmptyContent>
        <Button>Add data</Button>
      </EmptyContent>
    </Empty>
  )
}
```

## Examples

### Outline

Use the `border` utility class to create an outline empty state.

<ComponentPreview name="empty-outline" />

### Background

Use the `bg-*` utilities to add a background to the empty state.

<ComponentPreview name="empty-background" />

### Input Group

Add an [InputGroup](/docs/components/input-group) component to `EmptyContent`.

<ComponentPreview name="empty-input-group" />

### RTL

<ComponentPreview name="empty-rtl" direction="rtl" />

### No city found

A pattern for [City Selector](/docs/components/city-selector) 's dependent city field before a province is chosen.

<ComponentPreview name="empty-city" />

## API Reference

<ApiReference title="EmptyMedia" rows={emptyMediaApi} />
