import type { ApiReferenceRow } from "@/components/api-reference"

export const spinnerApi: ApiReferenceRow[] = [
  {
    prop: "className",
    type: "string",
    default: "—",
    description: "Set the size of the spinner, e.g. size-6.",
  },
]
