import type { ApiReferenceRow } from "@/components/api-reference"

export const skeletonApi: ApiReferenceRow[] = [
  {
    prop: "className",
    type: "string",
    default: "—",
    description: "Set the size and shape of the placeholder, e.g. h-4 w-40.",
  },
]
