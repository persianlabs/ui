import type { ApiReferenceRow } from "@/components/api-reference"

export const kbdApi: ApiReferenceRow[] = [
  {
    prop: "className",
    type: "string",
    default: "—",
    description: "Additional classes to apply to the key.",
  },
]
