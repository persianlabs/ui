import type { ApiReferenceRow } from "@/components/api-reference"

export const tomanIconApi: ApiReferenceRow[] = [
  {
    prop: "className",
    type: "string",
    default: "—",
    description:
      "Sizes and colors the icon like a lucide icon, e.g. size-4 text-muted-foreground.",
  },
]
